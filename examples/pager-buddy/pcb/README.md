# pager-buddy — pcb

> **Stub.** Sources to be filled in via [`vibe-pcb`](../../../skills/vibe-pcb/)
> (generate-by-script: `gen_sch.py` / `gen_pcb.py` — never hand-edit the `.kicad_*`).

Carrier for the MCU + OLED + buzzer + button. Belly keep-out under the flush module;
USB-C out one edge.

## Interface contracts (this domain is the producer)

- **Produces** [`pinmap.yaml`](pinmap.yaml) → consumed by `firmware/` (the net map).
- **Produces** `board.step` → consumed by `cad/` (the 0 mm³ fit-check). Generated by
  the pcb 3D/fab step; not a hand-edited file (gitignore lets `models/*.step` track).
- **Reads** [`../cad/constraints.yaml`](../cad/constraints.yaml) — the board outline,
  mount holes, and connector positions come from the shared fit numbers.

## Check *(fill in)*

`scripts/pcb_check.sh pager-buddy` → ERC/DRC 0 errors / 0 unconnected
(+ `BELLY_BOX=…` for the flush module's belly keep-out).
